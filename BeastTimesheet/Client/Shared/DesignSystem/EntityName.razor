@namespace BeastTimesheet.DesignSystem

#@Id - @(Name is null or "" ? "Draft" : Name)

@code {
    [Parameter, EditorRequired]
    public int Id { get; set; }
    [Parameter, EditorRequired]
    public string? Name { get; set; }
}